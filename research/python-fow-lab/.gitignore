.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.DS_Store

# Pre-foundations research outputs — superseded by lab/.
distill/

# Bake-off / feedback dirs (large, regenerable).
feedback/
tournaments/

# Lab run dirs are job outputs, regenerable from spec.json. Commit selectively
# (e.g., milestone runs) by adding the specific path with -f.
lab/runs/

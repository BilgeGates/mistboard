# Experiments

Use this directory for scratch scripts, notebooks, and logs.

Promotion rule: if an experiment becomes reusable, move the stable primitive into
`src/fow_chess/` and keep a short note here linking to the result.


170ab43
# ^ The mistboard-engine commit the prod build checks out (railpack build step).
# Dark Xiangqi engine path (2026-06-23): 170ab43 includes the DXQ royal-flight
# commit guard from 1391b5c plus the latest engine main bughouse checkpoint.
# Rollback target: 8b4935b (Misty 1.5 + DMX, before full Dark Xiangqi). Single
# source of truth for the deployed private engine; bump this + railpack cachebust
# together before deploying. The railpack reads only the first line; this comment
# is ignored.
# NOTE: the web service has MISTBOARD_BUILD_ENGINE=0 (it does not clone/build this
# repo); it fetches PREBUILT engine binaries from public GitHub releases (banqi +
# jungle-flip). engine.ref only matters to the engine-worker deploy.

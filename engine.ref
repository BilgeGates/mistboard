8d689ae
# ^ The mistboard-engine commit the prod build checks out (railpack build step).
# 2026-06-30: bumped 170ab43 -> 8d689ae (branch agent/jungle-flip-uci = engine main
# 316ee5b + the standalone jungle-flip-engine UCI crate) to ship the Flip Jungle bot.
# All changes vs 170ab43 are ADDITIVE jungle/jungle-flip engine work (new
# src/fow_chess/jungle_flip/, jungle_flip_rust, jungle-flip-engine crate); no edits to
# existing fog-engine files, so the Misty fog/DXQ engines are unaffected.
# Rollback target: 170ab43 (pre jungle-flip-engine; Dark Xiangqi royal-flight guard).
# Single source of truth for the deployed private engine; bump this + railpack
# cachebust together before deploying. The railpack reads only the first line.

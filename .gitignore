node_modules
dist
.env
.env.*
!.env.example
*.log
.DS_Store
apps/web/.vite
.venv
__pycache__
*.egg-info
research/python-fow-lab/corpora/*/games/
research/python-fow-lab/corpora/*/views/

# Local-only symlink for the ?bakeoff browser route. Points at a bake-off
# output directory under /tmp; not part of the repo.
apps/web/public/bakeoff

# Versioned bake-off output dirs (per Tier-1 release). Local development
# artifact only — multi-MB JSONL traces + per-game event logs the engine
# lab viewer reads. Regenerate with research/python-fow-lab/scripts/bake_off.py.
apps/web/public/bakeoff-*/

# Tournament run output dirs (timestamped per-run game logs + manifests,
# multi-MB each). Local-only artifact. Regenerate with
# research/python-fow-lab/scripts/tournament.py.
research/python-fow-lab/tournaments/

# Private founder/operator notes. Public docs should be contributor-safe.
docs-private/
.claude/

diff --git a/src/main.cpp b/src/main.cpp
index 0e1c959..c3877e0 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -21,9 +21,19 @@
 #include "thread.h"
 #include "uci.h"
 
+#ifdef __EMSCRIPTEN__
+#include <emscripten/emscripten.h>
+#endif
+
 using namespace Stockfish;
 
-int main(int argc, char* argv[]) {
+namespace {
+
+void initialize_engine(int argc, char* argv[]) {
+  static bool initialized = false;
+  if (initialized)
+      return;
+  initialized = true;
 
   std::cout << engine_info() << std::endl;
 
@@ -36,9 +46,37 @@ int main(int argc, char* argv[]) {
   Threads.set(size_t(Options["Threads"]));
   Search::clear(); // After threads are up
   Eval::NNUE::init();
+}
+
+}
+
+#ifdef __EMSCRIPTEN__
+extern "C" {
+
+EMSCRIPTEN_KEEPALIVE
+void pikajieqi_initialize() {
+  char program[] = "PikaJieQi";
+  char* argv[] = {program, nullptr};
+  initialize_engine(1, argv);
+}
+
+EMSCRIPTEN_KEEPALIVE
+void pikajieqi_command(const char* command) {
+  if (command)
+      UCI::command(command);
+}
+
+}
+#endif
+
+int main(int argc, char* argv[]) {
+
+  initialize_engine(argc, argv);
 
+#ifndef __EMSCRIPTEN__
   UCI::loop(argc, argv);
 
   Threads.set(0);
+#endif
   return 0;
 }
diff --git a/src/uci.cpp b/src/uci.cpp
index 634fe87..8382b6a 100644
--- a/src/uci.cpp
+++ b/src/uci.cpp
@@ -262,30 +262,30 @@ namespace {
 } // namespace
 
 
-/// UCI::loop() waits for a command from the stdin, parses it and then calls the appropriate
-/// function. It also intercepts an end-of-file (EOF) indication from the stdin to ensure a
-/// graceful exit if the GUI dies unexpectedly. When called with some command-line arguments, 
-/// like running 'bench', the function returns immediately after the command is executed.
-/// In addition to the UCI ones, some additional debug commands are also supported.
-
-void UCI::loop(int argc, char* argv[]) {
-
-  Position pos;
-  string token, cmd;
-  StateListPtr states(new std::deque<StateInfo>(1));
-
-  pos.set(StartFEN, &states->back(), Threads.main());
-
-  for (int i = 1; i < argc; ++i)
-      cmd += std::string(argv[i]) + " ";
-
-  std::vector<Move> banmoves = {};
-
-  do {
-      if (argc == 1 && !getline(cin, cmd)) // Wait for an input or an end-of-file (EOF) indication
-          cmd = "quit";
-
-      istringstream is(cmd);
+struct UciSession {
+  Position pos;
+  StateListPtr states{new std::deque<StateInfo>(1)};
+  std::vector<Move> banmoves = {};
+
+  UciSession() {
+    pos.set(StartFEN, &states->back(), Threads.main());
+  }
+};
+
+UciSession& uci_session() {
+  static UciSession session;
+  return session;
+}
+
+bool UCI::command(const std::string& cmd) {
+
+      auto& session = uci_session();
+      auto& pos = session.pos;
+      auto& states = session.states;
+      auto& banmoves = session.banmoves;
+      string token;
+
+      istringstream is(cmd);
 
       token.clear(); // Avoid a stale if getline() returns nothing or a blank line
       is >> skipws >> token;
@@ -343,11 +343,32 @@ void UCI::loop(int argc, char* argv[]) {
                        "\nthe Universal Chess Interface (UCI) protocol to communicate with a GUI, an API, etc."
                        "\nFor any further information, visit https://github.com/PikaCat-OuO/Pikafish#readme"
                        "\nor read the corresponding README.md and Copying.txt files distributed along with this program.\n" << sync_endl;
-      else if (!token.empty() && token[0] != '#')
-          sync_cout << "Unknown command: '" << cmd << "'. Type help for more information." << sync_endl;
-
-  } while (token != "quit" && argc == 1); // The command-line arguments are one-shot
-}
+      else if (!token.empty() && token[0] != '#')
+          sync_cout << "Unknown command: '" << cmd << "'. Type help for more information." << sync_endl;
+
+      return token == "quit";
+}
+
+
+/// UCI::loop() waits for a command from the stdin, parses it and then calls the appropriate
+/// function. It also intercepts an end-of-file (EOF) indication from the stdin to ensure a
+/// graceful exit if the GUI dies unexpectedly. When called with some command-line arguments,
+/// like running 'bench', the function returns immediately after the command is executed.
+/// In addition to the UCI ones, some additional debug commands are also supported.
+
+void UCI::loop(int argc, char* argv[]) {
+
+  string cmd;
+
+  for (int i = 1; i < argc; ++i)
+      cmd += std::string(argv[i]) + " ";
+
+  do {
+      if (argc == 1 && !getline(cin, cmd)) // Wait for an input or an end-of-file (EOF) indication
+          cmd = "quit";
+
+  } while (!UCI::command(cmd) && argc == 1); // The command-line arguments are one-shot
+}
 
 
 /// UCI::value() converts a Value to a string by adhering to the UCI protocol specification:
diff --git a/src/uci.h b/src/uci.h
index c4a4200..79db5a3 100644
--- a/src/uci.h
+++ b/src/uci.h
@@ -68,6 +68,7 @@ private:
 };
 
 void init(OptionsMap&);
+bool command(const std::string&);
 void loop(int argc, char* argv[]);
 std::string value(Value v);
 std::string square(Square s);
